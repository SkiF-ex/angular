import { FreshPostDirective } from './fresh-post.directive';

describe('FreshPostDirective', () => {
  it('should create an instance', () => {
    const directive = new FreshPostDirective();
    expect(directive).toBeTruthy();
  });
});
